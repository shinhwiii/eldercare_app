name: eldercare_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core & Auth (버전 명확히 고정)
  firebase_core: 2.25.4
  firebase_auth: 4.12.0

  # 로그인 UI 등 기본 위젯용
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
